  <!-- Work Detail Section -->
        <section class="ptb ptb-sm-80">
            <div class="container">
                <!-- work Filter -->
                <div class="row">
                    <ul class="container-filter categories-filter">
                        <li><a class="categories active" data-filter="*">All</a></li>
                        <li><a class="categories" data-filter=".illustration">Illustration</a></li>
                        <li><a class="categories" data-filter=".photography">Photography</a></li>
                        <li><a class="categories" data-filter=".graphicdesign">Graphic Design</a></li>
                    </ul>
                </div>
                <!-- End work Filter -->
                <div class="row container-masonry nf-col-3">


                    <% @portfolio.each do |portfolio| %>
                        <div class="nf-item spacing ruby <%= portfolio.tags %>">
                            <div class="item-box">
                                <%= link_to portfolio.image_upload.url(:large), class: 'cbox-gallary1', title: portfolio.name do %>
                                    <%= image_tag portfolio.image_upload.url(:medium), class: 'item-container', alt: '1' %>
                                    <div class="item-mask">
                                        <div class="item-caption">
                                            <h5 class="white"><%= portfolio.description %></h5>
                                            <p class="white"><%= portfolio.tags %></p>
                                        </div>
                                    </div>
                                <% end %>
                            </div>
                        </div>
                    <% end %>

                </div>

            </div>

        </section>
        <!-- End Work Detail Section -->